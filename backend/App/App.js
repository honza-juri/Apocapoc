const express = require('express');
const app = require('./index');
const config = require('./config')
const path = require('path')
const dotenv = require('dotenv').config;

app.listen(, () => {
    console.log(`A szerver fut | http://localhost:${}`);
})